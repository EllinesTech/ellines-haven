# Firebase Functions params (copy to functions/.env for local emulator — gitignored).
# Production: set via `firebase functions:secrets:set` / params, or deploy-time env.
#
# RESEND_API_KEY is a Secret — do not put it here; use:
#   firebase functions:secrets:set RESEND_API_KEY
#
RESEND_FROM=Ellines Haven <noreply@haven.ellines.co.ke>
LEADS_NOTIFY_EMAIL=tech@ellines.co.ke
ORDERS_NOTIFY_EMAIL=tech@ellines.co.ke
CAREERS_NOTIFY_EMAIL=info@ellines.co.ke
