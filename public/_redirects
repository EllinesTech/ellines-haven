# Missing hashed assets must NOT fall through to the SPA shell.
# Serving index.html for .js/.css causes "Unable to preload CSS" / MIME crashes.
# Existing files under /assets/ are still served normally by Cloudflare Pages.
/assets/*  /404.html  404

# All other routes → SPA shell
/*    /index.html   200
