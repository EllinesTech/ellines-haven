# Auto-deploy Ellines Haven frontend to Cloudflare Pages on push to main.
# Mirrors Ellines Tech Website (deploy-pages.yml). Firebase rules/functions
# stay in deploy.yml — this workflow only builds + uploads `dist/`.
#
# Required GitHub Actions secrets:
#   CLOUDFLARE_API_TOKEN   Account API token with Pages Edit
#   CLOUDFLARE_ACCOUNT_ID  Cloudflare account id
#
# Optional: attach Git in the dashboard instead, then disable this workflow
# to avoid double deploys.
#   dash.cloudflare.com → Workers & Pages → ellines-haven → Settings → Builds
#   → Connect to Git → branch main, build `npm run build`, output `dist`
#
# CRITICAL — dual deploy race (root cause of vendor-*.js / index-*.js 404s):
# Use ONLY this workflow OR Cloudflare Git builds — never both.
# If dash.cloudflare.com → Workers & Pages → ellines-haven → Settings → Builds
# shows a connected Git repo, disconnect it (or disable automatic deployments).
# Two builders of the same push produce different Vite content hashes; the
# custom domain can serve HTML from build A while origin assets are already
# build B → ERR_ABORTED 404 on vendor-B63*.js / vendor-firebase-core-*.js.
#
# Zone cache tip (custom domain haven.ellines.co.ke):
#   Caching → Configuration → Browser Cache TTL = "Respect Existing Headers"
#   Avoid zone Cache Rules that set long TTLs on /* or /assets/* 404s.

name: Deploy Cloudflare Pages

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - '.claude/**'
      - 'functions/**'
      - 'firestore.rules'
  workflow_dispatch:

concurrency:
  # Never cancel an in-flight Pages upload — partial deploys cause hashed
  # chunk 404s while index.html already references the new build.
  group: pages-production
  cancel-in-progress: false

jobs:
  deploy:
    name: Build and deploy
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install
        # package-lock.json is gitignored in this repo — use npm install, not npm ci
        run: npm install

      - name: Build
        run: npm run build
        env:
          VITE_PAYSTACK_PUBLIC_KEY: ${{ secrets.VITE_PAYSTACK_PUBLIC_KEY }}
          VITE_ENV_MODE: production

      - name: Verify hashed assets exist
        run: |
          set -e
          test -f dist/index.html
          test -f dist/_headers
          test -f dist/_redirects
          test -f dist/version.json
          count=$(find dist/assets -type f -name '*.js' | wc -l)
          echo "JS assets in dist/assets: $count"
          test "$count" -ge 10
          # Every script/modulepreload/stylesheet in index.html must exist on disk.
          # Entry-only checks miss missing vendor-*.js / vendor-firebase-core-*.js.
          mapfile -t refs < <(grep -oE '(src|href)="/assets/[^"]+\.(js|css)"' dist/index.html | grep -oE '/assets/[^"]+\.(js|css)' | sort -u)
          test "${#refs[@]}" -ge 4
          for ref in "${refs[@]}"; do
            test -f "dist${ref}"
            echo "OK dist${ref}"
          done
          html_stamp=$(grep -oE '<!-- build:[0-9]{14} -->' dist/index.html | head -1 | grep -oE '[0-9]{14}')
          ver_stamp=$(node -p "JSON.parse(require('fs').readFileSync('dist/version.json','utf8')).v")
          echo "HTML stamp=$html_stamp version.json=$ver_stamp"
          test -n "$html_stamp"
          test "$html_stamp" = "$ver_stamp"

      # ./functions is Firebase Cloud Functions. Wrangler treats ./functions as
      # Pages Functions and fails — stage a clean dist-only tree first.
      # pages-functions/ holds the asset-404 cache middleware (not Firebase).
      - name: Stage clean dist for Pages
        run: |
          rm -rf .cf-deploy
          mkdir -p .cf-deploy
          cp -R dist .cf-deploy/dist
          cp -R pages-functions .cf-deploy/functions

      - name: Deploy to Cloudflare Pages
        id: pages_deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          # Run from .cf-deploy so Wrangler does not scan repo ./functions
          workingDirectory: .cf-deploy
          command: pages deploy dist --project-name=ellines-haven --commit-dirty=true --branch=main

      - name: Smoke-check production assets
        run: |
          set -e
          sleep 8
          mapfile -t refs < <(grep -oE '(src|href)="/assets/[^"]+\.(js|css)"' dist/index.html | grep -oE '/assets/[^"]+\.(js|css)' | sort -u)
          # Verify on the Pages deployment origin first — custom-domain CDN can
          # still return HIT 200 for deleted hashes while HTML already moved on.
          DEPLOY_URL="${{ steps.pages_deploy.outputs.deployment-url }}"
          if [ -z "$DEPLOY_URL" ]; then
            DEPLOY_URL=$(npx wrangler pages deployment list --project-name=ellines-haven --json 2>/dev/null | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const j=JSON.parse(d);console.log((j[0]&&j[0].url)||'');});")
          fi
          echo "Deployment URL: $DEPLOY_URL"
          test -n "$DEPLOY_URL"
          # Status 200 alone is not enough — SPA/404.html fallback is text/html and
          # breaks CSS ("MIME type text/html") while looking like a soft miss.
          check_asset() {
            local base="$1" ref="$2" label="$3"
            local max_attempts="${4:-8}"
            local retry_sleep="${5:-5}"
            local want='javascript'
            [[ "$ref" == *.css ]] && want='text/css'
            local ok=0
            for (( attempt=1; attempt<=max_attempts; attempt++ )); do
              headers=$(curl -sSI "${base%/}${ref}" | tr -d '\r' || true)
              code=$(printf '%s\n' "$headers" | awk 'NR==1{print $2}')
              ctype=$(printf '%s\n' "$headers" | awk -F': ' 'tolower($1)=="content-type"{print $2; exit}')
              echo "${label} GET ${ref} → ${code} ctype=${ctype} (try ${attempt}/${max_attempts})"
              if [ "$code" = "200" ] && printf '%s' "$ctype" | grep -qi "$want"; then
                if printf '%s' "$ctype" | grep -qi 'text/html'; then
                  echo "rejecting HTML fallback for ${ref}"
                else
                  ok=1
                  break
                fi
              fi
              if [ "$attempt" -lt "$max_attempts" ]; then
                sleep "$retry_sleep"
              fi
            done
            test "$ok" = "1"
          }
          for ref in "${refs[@]}"; do
            check_asset "$DEPLOY_URL" "$ref" ORIGIN
          done
          # Custom-domain CDN can lag pages.dev for new hashed assets after deploy.
          echo "Waiting for custom-domain CDN before LIVE checks..."
          sleep 30
          for ref in "${refs[@]}"; do
            check_asset "https://haven.ellines.co.ke" "$ref" LIVE 12 10
          done
          html_cc=$(curl -sSI "https://haven.ellines.co.ke/" | tr -d '\r' | grep -i '^cache-control:' | head -1)
          echo "index Cache-Control: $html_cc"
          echo "$html_cc" | grep -qi 'max-age=0\|must-revalidate\|no-cache'
          miss_cc=$(curl -sSI "https://haven.ellines.co.ke/assets/__eh_missing_chunk__.js" | tr -d '\r' | grep -iE '^(cache-control|cdn-cache-control):' || true)
          echo "missing asset headers:"$'\n'"$miss_cc"
          # Must not advertise year-long / immutable caching on a 404
          if echo "$miss_cc" | grep -qi 'immutable\|max-age=31536000'; then
            echo "::warning::Missing asset still advertises long cache — check zone Cache Rules"
          fi
